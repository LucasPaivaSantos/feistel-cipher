export interface Inputs {
  key: number;
  clearText: string;
}

// const inputsFormData = (inputs: Inputs): FormData => {
//   const formData = new FormData();

//   formData.append("key", inputs.key.toString());
//   formData.append("clearText", inputs.clearText);

//   return formData;
// };
